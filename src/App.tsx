import React from "react";
import { Header, PhotoFrame } from "./components";

const App = (): JSX.Element => {
  return (
    <div className="App">
      <Header />
      <PhotoFrame />
    </div>
  );
};

export default App;
